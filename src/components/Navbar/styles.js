import styled from "styled-components";

import Constants from "../../util/Constants";

export const Header = styled.div``;

export const Navigation = styled.nav`
  background: ${Constants.colors.SMOKE};  
  padding: 20px;
  
  a {
    margin: 20px;    
  }
`;